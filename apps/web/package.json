{
  "name": "@payrecord/web",
  "version": "0.1.0",
  "private": true,
  "description": "PayRecord public website: product info, release updates, downloads, privacy, terms, support.",
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "pnpm --filter @payrecord/contracts run build && pnpm --filter @payrecord/receipt-parsers run build && next build",
    "start": "next start -p 3001",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"web: static content; verified by next build\" && exit 0"
  },
  "dependencies": {
    "@payrecord/contracts": "workspace:*",
    "@payrecord/receipt-parsers": "workspace:*",
    "next": "^16.3.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.6.3"
  }
}
